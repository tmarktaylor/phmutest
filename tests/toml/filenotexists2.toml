[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = ["tests/md/setupnoteardown.md", "tests/md/sharedto.md"]
exclude-globs = []
fixture = ""
share-across-files = ["tests/md/setupnoteardown.md"]
setup-across-files = ["tests/md/setupnoteardown.md", "NotAnExistingFile2.md"]
# Only one of select, deselect may be non-empty.
select = []
deselect = []
skip = []

