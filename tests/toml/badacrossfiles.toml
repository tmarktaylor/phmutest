[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = [
    "tests/md/project.md",
    "tests/md/directive2.md",
    "tests/md/unexpected_output.md"
]
# exclude a matching include-globs file
exclude-globs = []
fixture = ""
share-across-files = ["not-existing-1.md"]
setup-across-files = ["not-existing-2.md"]
# Only one of select, deselect may be non-empty.
select = []
deselect = []
skip = []

