[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = ["tests/md/sharedto.md"]
exclude-globs = []
fixture = ""
share-across-files = ["tests/md/setupnoteardown.md"]
setup-across-files = []
# Only one of select, deselect may be non-empty.
select = []
deselect = []
skip = []
color = false
style = ""

