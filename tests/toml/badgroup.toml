[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = ["tests/md/code_groups.md"]
exclude-globs = []
fixture = ""
share-across-files = []
setup-across-files = []
# Only one of select, deselect may be non-empty.
select = ["group-1"]
deselect = ["group-4"]
skip = []

