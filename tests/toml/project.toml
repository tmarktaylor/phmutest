[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = [
    "tests/md/project.md",
    "tests/md/directive1.md",
    "tests/md/unexpected_output.md"
]
exclude-globs = []
fixture = ""
share-across-files = []
setup-across-files = []
# Only one of select, deselect may be non-empty.
select = []
deselect = []
skip = []

