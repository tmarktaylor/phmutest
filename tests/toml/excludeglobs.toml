[tool.phmutest]
# Use fnmatch like patterns on the -globs keys
include-globs = [
    "tests/md/project.md",
    "tests/md/directive1.md",
    "tests/md/un*_output.md"  # wildcard
]
# exclude a matching include-globs file
exclude-globs = ["tests/md/unexpected_*.md"]  # wildcard
fixture = ""
share-across-files = []
setup-across-files = []
# Only one of select, deselect may be non-empty.
select = []
deselect = []
skip = []

